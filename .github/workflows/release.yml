name: Build and release station-board

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        arch:
          - x86_64
          - arm64

    steps:
      - name: Check out tagged source
        uses: actions/checkout@v7
        with:
          ref: ${{ github.ref }}
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.14'
          cache: pip

      - name: Install Tkinter build dependency
        run: sudo apt-get update && sudo apt-get install --yes python3-tk

      - name: Install Python dependencies
        run: python -m pip install --upgrade pip && python -m pip install -r requirements.txt

      - name: Embed version from tag
        run: echo "__version__ = '${{ github.ref_name }}'" > src/_version.py

      - name: Build application with PyInstaller
        run: pyinstaller --clean --noconfirm --name station-board --onefile --windowed src/main.py --target-arch ${{ matrix.arch }}

      - name: Package application
        env:
          TAG_NAME: ${{ github.ref_name }}
        run: tar -czf "station-board-${TAG_NAME}-linux-${{ matrix.arch }}.tar.gz" -C dist station-board

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ github.ref_name }}
        run: |
          gh release create "$TAG_NAME" \
            "station-board-${TAG_NAME}-linux-${{ matrix.arch }}.tar.gz" \
            --title "$TAG_NAME" \
            --generate-notes
